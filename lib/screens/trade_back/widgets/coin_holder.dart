import 'package:flutter/material.dart';

class CoinHolder extends StatelessWidget {
  const CoinHolder({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Coin Holder");
  }
}
